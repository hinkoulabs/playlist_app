<%= simple_form_for playlist do |f| %>
  <%= f.input :name %>
  <%= f.button :submit, t("helpers.submit.create"), class: 'btn btn-primary mx-1' %>
  <%= link_to t("actions.cancel"), playlist_path, class: 'btn btn-secondary' %>
<% end %>